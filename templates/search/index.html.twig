{% extends "base.html.twig" %}

{% block title %}
    Recherches | Progica
{% endblock %}

{% block body %}
    {{ render(controller('App\\Controller\\SearchController::searchBar')) }}

    {% if app.request.method == 'POST' %}
        <div>
            {% if gites | length == 0 %}
                <h4>Aucun résulat pour votre recherche.</h4>
            {% else %}
            {# {{ dump(gites) }} #}
                <h3 class="mt-3">Vos résulats de recherche :</h3>
                {% for gite in gites %}
                    <div class="mt-3 p-3 bg-light border rounded">
                        {# <a href="{{ path('gite_show', {'slug': gite.slug}) }}"> #}
                                <h6 class="text-dark"><i class="bi bi-arrow-down-right-square-fill text-primary"></i> {{ gite.couchage }}</h6>
                                <div class="text-dark small p-1 rounded">
                                        {# Posté le {{ gite.createdAt|format_datetime('medium', 'short') }} par {{ gite.author }} #}
                                </div>
                        </a>
                    </div>
                {% endfor %}
            {% endif %}
        </div>
    {% endif %}

{% endblock %}

